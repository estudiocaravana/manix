---
export interface Props {
  es: string;
  en: string;
  gl: string;
  prezo: string;
}

const { es, en, gl, prezo } = Astro.props;
---

<div class="flex mt-6 py-3 border-t border-dotted border-indigo-900">
  <div>{es}</div>
  <div class="grow text-right">{prezo} €</div>
</div>
